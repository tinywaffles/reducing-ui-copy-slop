interface:
  display_name: "Reducing UI Copy Slop"
  short_description: "Remove bloated and repetitive interface copy"
  default_prompt: "Use $reducing-ui-copy-slop to audit this interface copy and keep only useful text."
